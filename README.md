# 🚀 Job Optimizer MVP

**Job Optimizer** — это AI-сервис для автоматического улучшения описаний вакансий. Система анализирует исторические данные, выявляет наиболее эффективные вакансии (Top Performers) и использует их структуру и стиль для переписывания новых вакансий с помощью LLM (Qwen-72B).

---

## 🏗 Архитектура
Проект состоит из трех основных модулей:
1.  **Data Pipeline (`src/data`)**:
    *   Анализирует сырые исторические данные (`fact_vacancies_raw.csv`).
    *   Рассчитывает метрику **"Peak Efficiency"** (максимальный прирост откликов за 7-дневное окно).
    *   Формирует базу знаний (`vacancies_processed.parquet`) из топ-20% лучших вакансий.

2.  **RAG Engine (`src/rag`)**:
    *   **Retriever**: Использует `rubert-tiny2` для векторного поиска похожих успешных вакансий.
    *   **LLM**: Использует модель `Qwen/Qwen2.5-72B-Instruct` через HuggingFace Inference API для генерации текста.

3.  **Application**:
    *   **Backend**: FastAPI (`src/api`) — REST API для обработки запросов.
    *   **Frontend**: Streamlit (`src/demo`) — веб-интерфейс для пользователей.
---

## 🛠 Установка и Настройка
Проект использует **Poetry** для управления зависимостями.

### 1. Предварительные требования
*   Python 3.10+
*   Poetry (установленный в системе)
*   Git

### 2. Клонирование и установка зависимостей
```bash
# Клонирование репозитория
git clone <your-repo-url>
cd job-optimizer-mvp

# Настройка Poetry (чтобы папка .venv создавалась внутри проекта)
poetry config virtualenvs.in-project true

# Установка всех библиотек (версии зафиксированы в poetry.lock)
poetry install

# Удаляем ненужную либу, чтобы не было конфликтов
poetry run pip uninstall -y torchvision
```

### 3. Настройка окружения (.env)
*   Создайте файл .env в корне проекта и добавьте ваш токен Hugging Face:
```ini
# .env
HUGGINGFACEHUB_API_TOKEN=hf_ВашТокенЗдесь
```

---

# 🚀 Запуск проекта

Порядок запуска важен: сначала данные, потом сервер, потом интерфейс.

**Шаг 1: Подготовка данных**

Скрипт рассчитает эффективность вакансий и создаст векторный индекс.

Требуется файл data/fact_vacancies_raw.csv.

```bash
poetry run python src/data/prepare.py
```

Ожидаемый результат: появление файла data/vacancies_processed.parquet.

**Шаг 2: Запуск Backend API**

Запускает сервер на порту 8000. При первом запуске будет построен поисковый индекс (займет 1-2 минуты).

```bash
poetry run python -m src.api.main
```

**Шаг 3: Запуск Frontend (в новом терминале)**

Запускает веб-интерфейс Streamlit.

```bash
poetry run streamlit run src/demo/app.py
```

---

# 🐳 Запуск в Docker

**Если вы хотите запустить проект в изолированном контейнере:**

1. Сборка образа:

```bash
docker build -t job-optimizer .
```

2. Запуск контейнера

```bash
docker run -p 8000:8000 -p 8501:8501 --env-file .env job-optimizer
```

---

# 🧠 Логика работы метрики (Efficiency)

Мы не используем простое количество откликов. Мы считаем динамику:

1. Для каждой вакансии берется история изменений (total_responses по дням).
2. Алгоритм сканирует историю скользящим окном в 7 дней.
3. Находится неделя с максимальным приростом откликов.
4. Если вакансия стартовала с 0 откликов, то прирост считается от первого дня, когда появился хотя бы 1 отклик (чтобы исключить "мертвые" периоды модерации).

---

# 📂 Структура проекта
```markdown
job-optimizer-mvp/
├── data/                       # Данные (CSV, Parquet, Index)
├── src/
│   ├── api/                    # Backend (FastAPI)
│   │   ├── main.py             # Точка входа API
│   │   └── models.py           # Pydantic схемы
│   ├── data/                   # ETL скрипты
│   │   └── prepare.py          # Расчет метрик и очистка
│   ├── demo/                   # Frontend (Streamlit)
│   │   └── app.py              # Веб-приложение
│   └── rag/                    # AI Логика
│       ├── llm.py              # Интеграция с Qwen (HuggingFace)
│       └── retriever.py        # Векторный поиск (SentenceTransformers)
├── pyproject.toml              # Зависимости проекта
├── poetry.lock                 # Фиксация версий библиотек
├── Dockerfile                  # Конфигурация Docker
└── README.md                   # Документация
```
